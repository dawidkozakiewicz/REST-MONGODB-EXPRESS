<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>
  <body>
    <%- include('./partials/nav.ejs') %>

    <div>
      <h2>All Blogs</h2>
      <% if (blogs.length > 0) { blogs.forEach(blog=> { %>
      <h3><%= blog.title %></h3>
      <p><%= blog.snippet %></p>
      <p>There is no blogs to display</p>
      <% })} else { %>
      <h1>There is no blogs to display</h1>
      <% } %>
    </div>
    <%- include('./partials/footer.ejs') %>
  </body>
</html>
